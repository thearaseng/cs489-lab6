spring:
  application:
    name: lab6
  datasource:
    url: ${DATASOURCE_URL:jdbc:mysql://localhost:3308/dental_database}
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
