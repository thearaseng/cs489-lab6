spring:
  application:
    name: lab6
  datasource:
    url: jdbc:mysql://localhost:3306/dental_database
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
